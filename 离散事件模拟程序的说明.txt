//链式队列 
QElemType作为队列数据元素的结构体，QNode队列结点，QueuePtr为队列结点指针，这里的结点指针值内确有指向结构体的指针
//事件表，线性表的链式存储
普通的定义方式，不赘述
//插入事件表函数：
46 int(*compare)(int,int))函数指针调用函数，compare为指针，定义一般格式：*指针名（参数类型）
！！！60 事件表非空->与链表第一个节点对比->若大于等于当前结点的数值则移向下一位，若下一位为最后一个结点，则进行插入，否则继续循环，直到第一次出现小于当前结点的数值则插入在前面。经典写法
108 L值的变化情况
在C语言中，函数形参是没有&修饰符的，这个&来自于C++，因为使用方便，且目前的大部分编译环境都支持C++，所以不讲究的人在C中使用了C++的&修饰符，在C++的函数形参表中，&修饰符表示对实参的引用，可以这样理解，通过&操作符，在主调函数和被调函数中，主调函数中的实参对于被调函数如同该函数局部变量一样的使用权限，就像全局变量一样。
192 计算最小队列算法挺经典
221 durtime = 1 + (int)(30.0*rand() / (RAND_MAX + 1.0))
RAND_MAX是VC系统定义的宏定义10.0*rand()得到一个浮点数
串事件表：第一个客户到达事件初始化事件表
事件表并不是一开始就建立完毕，而是在第一个用户到达后开始进行完善，在调用函数Bank中进行链表的删除操作，并指向下一位，ev链表是全局变量，